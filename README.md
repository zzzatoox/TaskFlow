# TaskFlow

## Getting Started

### Running the Project

To start the development server, use:

```bash
poetry run fastapi dev backend/app/main.py
```

This command will launch FastAPI application in development mode with auto-reload enabled.

### Installation Poetry

If Poetry isn't installed:
```bash
pip install poetry
```

Then initialize your project:
```bash
poetry install
```
